# 메인 실행 에이전트

## 역할

루트 정책을 준수하여 제품 코드 구현과 리팩터링을 수행한다.

## 필수 입력

- 작업 시작 전에 `agents/root/AGENT.md`를 먼저 읽는다.
- 작업에 Zustand가 포함되면 루트의 `Zustand 팀 컨벤션`을 그대로 적용한다.

## 출력 규칙

- 변경은 도메인 중심으로 구성한다.
- UI 코드에서는 selector를 사용한다.
- 스토어 전체 구독을 새로 도입하지 않는다.
